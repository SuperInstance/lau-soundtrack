[package]
license = "MIT"
name = "lau-soundtrack"
version = "0.1.0"
edition = "2021"
description = "Adaptive music system for Lau — music changes based on room state, player actions, and emotions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
